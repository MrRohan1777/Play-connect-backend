spring.application.name=PlayConnect
server.port=${PORT:8080}
# ===============================
# = DATA SOURCE
# ===============================

#spring.datasource.url=jdbc:postgresql://localhost:5432/PlayConnect
#spring.datasource.username=postgres
#spring.datasource.password=Postgres@2001
spring.datasource.url=jdbc:postgresql://ep-wandering-frost-a1kklp0u-pooler.ap-southeast-1.aws.neon.tech/neondb
spring.datasource.username=neondb_owner
spring.datasource.password=npg_yNgOtLZvCJ23
spring.datasource.driver-class-name=org.postgresql.Driver


# ===============================
# = JPA / HIBERNATE
# ===============================

spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true


# ===============================
# = CONNECTION POOL (Hikari - Default)
# ===============================

spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.connection-timeout=20000

# ===============================
# = MAIL (Forgot/Reset Password)
# ===============================
# Configure these for real emails (Gmail/SendGrid/etc)
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=thinggems@gmail.com
spring.mail.password=onfz xxlh qjux kslt
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# From address used for outgoing emails (must be a valid email format)
app.mail.from=${MAIL_FROM:no-reply@playconnect.local}

# Link target used in forgot-password email
app.password-reset.frontend-url=${PASSWORD_RESET_FRONTEND_URL:http://localhost:5173/reset-password}
# Token validity in minutes
app.password-reset.token-ttl-minutes=${PASSWORD_RESET_TTL_MINUTES:30}
